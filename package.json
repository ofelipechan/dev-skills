{
  "name": "dev-skills-monorepo",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w dev-skills",
    "test": "npm test -w dev-skills",
    "typecheck": "npm run typecheck -w dev-skills",
    "generate:registry": "tsx scripts/generate-registry.ts",
    "check:registry": "tsx scripts/generate-registry.ts --check"
  },
  "devDependencies": {
    "@types/node": "22.20.3",
    "tsx": "4.23.13",
    "typescript": "5.9.3"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ]
}
