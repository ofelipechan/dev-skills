{
  "name": "dev-skills-monorepo",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w @ofelipechan/dev-skills",
    "test": "npm test -w @ofelipechan/dev-skills",
    "typecheck": "npm run typecheck -w @ofelipechan/dev-skills",
    "generate:registry": "tsx scripts/generate-registry.ts",
    "check:registry": "tsx scripts/generate-registry.ts --check"
  },
  "devDependencies": {
    "@types/node": "22.20.3",
    "tsx": "4.23.13",
    "typescript": "5.9.3"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ]
}
